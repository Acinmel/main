# Roadmap

## Emergency Focus: Core Flow Recovery

当前路线临时收敛为“先让主链路在本地重新稳定跑通”。在 `QA-CORE-001` 通过前，所有新增功能、模板扩展、后端瘦身、线上发布准备都降级为非执行项。

核心产品路径：

1. 提取爆款文案：从链接提取或手动整理口播文案，创建 `video_projects` 任务并保存 `scriptHash`。
2. 克隆/选择声音并生成音频：生成 `audioAssetId`，可试听，语速可控。
3. 对字幕时间轴：按当前口播分段生成 `subtitleTrackId`，返回 `source=tts_alignment`，每段有秒级 `startTime/endTime`。
4. 生成口型视频：按当前 `projectId + audioAssetId + avatarResourceId` 生成 `digitalHumanVideoAssetId`，状态可查询、可恢复，不复活旧结果。
5. 渲染最终视频：用户选择字幕模板，最终包装只消费当前口型视频、当前字幕轨和当前模板样式，输出可预览成片。

验收顺序：本地端到端 -> staging 同样流程 -> 再考虑生产发布。生产发布、真实付费 provider 调用和破坏性数据库变更仍需人工确认。

## Current Milestone

V1.0 后优化与稳定性迭代。线上主流程已经跑通，当前目标不是重做 MVP，而是在不破坏现有链路的前提下完善留存、模板、权限、预览、测试环境和部署可靠性。

## Track 1: Studio Staged Workflow

目标：让创作流程拆成清晰的任务、音频、口型、包装四个阶段，并且每个阶段都能恢复。

- 任务阶段：第一步文案确认后创建 `video_projects` 创作任务，用户可编辑任务名，后续步骤使用真实 `projectId`。
- 音频阶段：生成或上传音频资产，生成秒级字幕时间轴。
- 口型阶段：基于当前用户的数字人视频和音频生成可复用口型视频。
- 包装阶段：只负责字幕、标题、音频和视频对齐，不再把所有工作塞到最终成片请求。
- 留存阶段：刷新页面、跳转页面或重新进入创作台后，按 `projectId` 恢复任务内容；不按音频、文案、数字人视频或链接匹配其他任务结果。

当前状态：三段式主体完成；`ARCH-016` 已确定创作任务容器方案，开发任务已进入看板；真实长任务边界测试待 staging/mock 或人工确认。

## Track 2: Resource Isolation And Permissions

目标：所有用户资源都只能被本人看到和使用，管理员负责开通普通用户权限。

- 数字人视频、音色、生成音频、字幕模板、标题素材、最近提取记录必须按用户隔离。
- 普通新注册用户默认不可使用核心功能，管理员开通后才能继续。
- 资源预览流接口必须校验当前用户权限，禁止通过 Network 直接访问他人资源。

当前状态：主体完成；后续新增资源类型必须沿用同一隔离规则。

## Track 3: Subtitle And Title Template Editing

目标：字幕、标题和封面模板支持“公版预览、复制后编辑、保存自用”。

- 公版模板只读，可预览、可复制，不可直接修改。
- 复制模板归属当前用户，可修改封面文字、字幕样式、标题样式、颜色、阴影、背景、位置、画幅等。
- 用户可视化拖拽字幕和标题位置，保存后的样式数据参与最终成片渲染。
- 标题素材使用透明背景资产叠加到主视频，失败不影响文案保存和字幕高亮。

当前状态：主体完成；后续重点是 UI 精简、模板预览性能和样式字段兼容。

## Track 4: Media Preview And Performance

目标：减少用户等待和重复生成，保证预览清晰稳定。

- 音频生成完成后尽快显示试听栏，失败时展示可理解错误。
- 视频选择后先显示轻量预览，再在后台拉取完整可播放地址。
- 口型生成和最终包装均使用异步任务、进度反馈和幂等复用。
- 对高成本 provider 调用使用去重、超时、失败态和显式重试。

当前状态：主体完成；真实生产视频压缩和色彩偏移仍需按具体 provider/ffmpeg 参数做专项验收。

## Track 5: Deployment And Test Environment

目标：新功能先在本地和 staging 验证，生产发布必须人工确认。

- 本地：前端 Vite、后端 Nest、SQLite、`/uploads` 代理。
- staging：使用非生产密钥、非生产数据库、独立上传目录，允许自动部署和 smoke test。
- production：发布、重启、回滚、数据库破坏性变更、真实付费调用必须人工确认。

当前状态：本地验证路径可用；staging 资源夹具和真实长任务测试仍待准备。

## Track 6: Creation Task Container

目标：用轻量 `video_projects` 统一承载一次创作，不再依赖固定 `studio-current` 或自动匹配旧资产。

- 用户从第一步点击下一步时创建任务，可编辑任务名。
- 任务列表按当前用户展示，可搜索、改名、归档和继续编辑。
- 创作台通过 `projectId` 恢复文案、音频、字幕、数字人选择、当前阶段和已生成资产。
- 任务名允许重复，只做展示和搜索，不参与数据关联。
- 现有 `task_statuses` 继续表示异步任务，不替代产品层创作任务。

当前状态：`ARCH-016` 通过；后端表/API、前端创建任务入口和恢复逻辑待开发。

## Not In Current Scope

- 推翻 V1.0 主链路重新设计。
- 未确认的付费 AI 调用或生产发布。
- 破坏性数据库变更。
- 为了单个 UI 问题进行大规模无关重构。
